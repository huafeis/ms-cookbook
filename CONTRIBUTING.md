# 参与魔搭紫皮书共建

欢迎贡献技术勘误、可复现案例、AIGC 创作经验与阅读体验改进。

## 两种投稿方式

1. **GitHub：** Fork [仓库](https://github.com/modelscope/ms-cookbook)，创建分支，完成修改后提交 Pull Request。请说明涉及章节、修改原因和验证结果。
2. **魔搭：** 打开 [开发者实践](https://modelscope.cn/spotlight)，选择「创建内容」，发布时选择专题 **#魔搭紫皮书**。

学习交流和共建群二维码可在网站「共学共建」页面找到。

## 内容放在哪里

- `chapters/chapter-01.html` 至 `chapter-31.html`：各章正文，保留图片、代码和公式的 HTML 格式。
- `chapters/manifest.json`：篇目、标题、状态、小节目录、图片与附件数量等元数据。
- `assets/manuscript-20260914/`：现有正文素材。新增素材可使用单独目录，HTML 中的素材路径以网站根目录为基准。
- `assets/content.js`：供网站加载的生成文件。请从上述稿件构建，避免单独手改。

修改完成后，在仓库根目录运行（需要 Node.js 22 或更新版本）：

```sh
node scripts/build-content.mjs
node scripts/build-content.mjs --check
python3 -m http.server 4173 --bind 127.0.0.1
```

打开 `http://127.0.0.1:4173`，检查改动涉及的章节、图片、代码、公式和手机布局。将稿件、元数据与生成的 `assets/content.js` 一起提交。

如果新增或调整小节标题，请同步 `manifest.json` 中的 `headings` 标签与锚点；若改变章节状态、阅读时长或素材数量，也请同步对应字段。构建器会核对目录锚点并重新汇总总章数、可读章数和素材数量。

## 场景案例

场景页收录围绕具体任务的案例。请说明：

- 任务与目标用户：解决谁的什么问题，输入和预期输出是什么。
- 运行条件：模型、工具版本、硬件、数据与环境准备。
- 实现步骤：可以复现的代码、配置与关键截图。
- 效果验证：示例结果、评价依据、局限与改进方向。

MCP、Skill、LoRA 等通用技术教程归入对应阅读路径；案例可引用这些技术。

## AIGC 创作

AIGC 是独立阅读路径，欢迎设计师与内容创作者参与。方向包括图像风格与主体保持、局部编辑、视频角色一致性与镜头工作流、音频与音乐创作，以及画质、语义一致性、人工偏好与品牌要求评测。请标明实际完成的范围；待补充章节继续保留待补充标识。

## 署名与素材

在 PR 或投稿中注明希望展示的公开名称、主页链接及贡献范围。共建者名录区分内容与网站协作、技术评审；评审署名应由本人或维护者确认。当前网站名单依据 GitHub 公开贡献记录整理，不等同于全部内容作者名单。

保留材料来源与许可证，确认具有分享权限。请勿提交凭据、私人数据或未经授权的客户资料。群二维码如失效，可替换 `assets/home/community-qr.png` 并核验手机扫码效果。
